export default getRulesSelectors;
/**
 * Get all selectors from a rules set.
 * @param {Rule[]} rules List of rules.
 * @returns {string[]} Selectors.
 */
declare function getRulesSelectors(rules: Rule[]): string[];
