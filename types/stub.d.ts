export default stub;
/**
 * Get `'foo'` or `'bar'`.
 * @param {boolean} [foo=false] Whatever to return `'foo'` instead of `'bar'`.
 * @returns {string} `'foo'` or `'bar'`.
 */
declare function stub(foo?: boolean): string;
